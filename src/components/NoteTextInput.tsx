"use client";

type Props = {
  noteId: string;
  startingNoteText: string;
};

const NoteTextInput = ({ noteId, startingNoteText }: Props) => {
  return <div className="">Note input</div>;
};

export default NoteTextInput;
